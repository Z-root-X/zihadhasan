rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // 🟢 Public Read: Everyone can see uploaded images
      allow read: if true;
      
      // 🔒 Admin Write: Only logged-in users can upload
      allow write: if request.auth != null;
    }
  }
}
